pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
               dir('bugtracker-backend') {
                sh '''
                    go version
                    go test -v ./...
                    '''
               }
            }
        }
        stage('Unit Tests - Frontend') {
            steps {
               dir('bugtracker-frontend') {
                sh '''
                    node --version
                    npm ci
                    npm test
                    '''
               }
            }
        }
    }
}
